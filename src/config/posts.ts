import ContentProps from "../types/ContentProps";

const posts: ContentProps[] = [
  {
    title: "Posts",
    description: "",
    points: {
      type: "decimal",
      data: [],
    },
  },
];

export default posts;
